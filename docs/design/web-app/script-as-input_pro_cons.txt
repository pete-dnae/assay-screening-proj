New-Backend-Model
Pros:
  o Simple data model on the backend.

Cons:
  o Replicating logic on both front, backend
    for text scripts.
  o Transformation of model to suit new use case
    might be difficult.
  o additional logic to maintain versioning if script language evolves.

Old-Backend-model
Pros:
  o All the logic stays in the front end .
Cons:
  o Need to do additional housekeeping to clean/map reagents.
  o Additional load on frontend to convert rules to script language.
